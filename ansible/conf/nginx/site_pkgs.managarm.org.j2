# {{ ansible_managed }}
{% import 'nginx_macros.j2' as h %}
server {
	listen 443 ssl http2;
	server_name pkgs.managarm.org;
	return 301 https://repos.managarm.org$request_uri;

	{{ h.ssl_keys("pkgs.managarm.org") }}
}

# removed.
