roles/distribution configuration
================================
This directory contains templates under the filenames ``{{ ansible_hostname
}}.j2`` which directly translate to ``/etc/approve-distro.toml`` on the hosts.
