# {{ ansible_managed }}
{% import 'nginx_macros.j2' as h %}
server {
	listen 443 ssl http2;
	listen 80;
	server_name ci.managarm.org;
	return 301 https://builds.managarm.org$request_uri;

	{{ h.ssl_keys("ci.managarm.org") }}
}

# removed.
